name: Blob storage upload

on:
    push:
        branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - uses: azure/login@v1
      with:
          creds: ${{ secrets.AZURE_STORAGE }}

    - name: Upload to blob storage
      uses: azure/CLI@v1
      with:
        inlineScript: |
            az storage blob upload-batch --account-name raju1603 --auth-mode key -d '$web' -s .
   

  # Azure logout
    - name: logout
      run: |
            az logout
      if: always()
